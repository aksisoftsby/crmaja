<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-xl font-semibold text-gray-800">{{ $lead->name }}</h2><p class="text-sm text-gray-500">{{ $lead->company_name ?: 'Lead individu' }}</p></div><div class="flex items-center gap-3"><a href="{{ route('leads.index') }}" class="text-sm text-gray-600">Kembali</a>@can('update', $lead)<a href="{{ route('leads.edit', $lead) }}" class="rounded-md bg-indigo-600 px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">Ubah</a>@endcan</div></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-5xl space-y-5 sm:px-6 lg:px-8">@if(session('status'))<div class="rounded-md bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif
        <div class="grid grid-cols-1 gap-5 lg:grid-cols-3"><section class="rounded-lg bg-white p-6 shadow-sm lg:col-span-2"><h3 class="border-b pb-4 font-semibold text-gray-900">Informasi lead</h3><dl class="mt-5 grid grid-cols-1 gap-5 text-sm sm:grid-cols-2"><div><dt class="text-gray-500">Email</dt><dd class="mt-1 font-medium">{{ $lead->email ?: '—' }}</dd></div><div><dt class="text-gray-500">Telepon</dt><dd class="mt-1 font-medium">{{ $lead->phone ?: '—' }}</dd></div><div><dt class="text-gray-500">Sumber</dt><dd class="mt-1 font-medium">{{ $lead->source?->name ?? '—' }}</dd></div><div><dt class="text-gray-500">Staf</dt><dd class="mt-1 font-medium">{{ $lead->assignedStaff?->name ?? '—' }}</dd></div><div><dt class="text-gray-500">Nilai estimasi</dt><dd class="mt-1 font-medium">{{ number_format((float)$lead->lead_value, 2, ',', '.') }}</dd></div><div><dt class="text-gray-500">Status</dt><dd class="mt-1"><span class="rounded-full px-2 py-1 text-xs font-medium text-white" style="background-color: {{ $lead->status->color }}">{{ $lead->status->name }}</span></dd></div><div class="sm:col-span-2"><dt class="text-gray-500">Catatan</dt><dd class="mt-1 whitespace-pre-line font-medium">{{ $lead->notes ?: '—' }}</dd></div></dl></section><aside class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Riwayat</h3><dl class="mt-4 space-y-3 text-sm"><div><dt class="text-gray-500">Dibuat oleh</dt><dd>{{ $lead->creator?->name ?? '—' }}</dd></div><div><dt class="text-gray-500">Dibuat pada</dt><dd>{{ $lead->created_at->translatedFormat('d M Y H:i') }}</dd></div><div><dt class="text-gray-500">Konversi</dt><dd>{{ $lead->is_converted ? 'Sudah dikonversi' : 'Belum dikonversi' }}</dd></div></dl>@can('delete', $lead)<form method="POST" action="{{ route('leads.destroy', $lead) }}" class="mt-6 border-t pt-4" onsubmit="return confirm('Arsipkan lead ini?')">@csrf @method('DELETE')<button class="text-sm font-medium text-red-600">Arsipkan lead</button></form>@endcan</aside></div>
    </div></div>
</x-app-layout>
