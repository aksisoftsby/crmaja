<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Leads</h2>@can('create', App\Models\Lead::class)<a href="{{ route('leads.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">Tambah lead</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 sm:px-6 lg:px-8">
        @if(session('status'))<div class="rounded-md bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif
        <div class="rounded-lg bg-white p-6 shadow-sm"><form method="GET" class="grid grid-cols-1 gap-3 md:grid-cols-4"><div class="md:col-span-2"><x-input-label for="search" value="Cari lead"/><x-text-input id="search" name="search" class="mt-1 block w-full" :value="request('search')" placeholder="Nama, perusahaan, email, atau telepon"/></div><div><x-input-label for="status_id" value="Status"/><select id="status_id" name="status_id" class="mt-1 block w-full rounded-md border-gray-300"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->id }}" @selected((string)request('status_id') === (string)$status->id)>{{ $status->name }}</option>@endforeach</select></div><div class="flex items-end gap-2"><x-primary-button>Filter</x-primary-button><a href="{{ route('leads.index') }}" class="text-sm text-gray-600">Reset</a></div></form></div>
        <div class="overflow-hidden rounded-lg bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left text-xs uppercase tracking-wider text-gray-500"><tr><th class="px-6 py-3">Lead</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Sumber</th><th class="px-6 py-3">Staf</th><th class="px-6 py-3">Nilai</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y divide-gray-200">@forelse($leads as $lead)<tr><td class="px-6 py-4"><a href="{{ route('leads.show', $lead) }}" class="font-semibold text-indigo-600">{{ $lead->name }}</a><div class="text-xs text-gray-500">{{ collect([$lead->company_name, $lead->email])->filter()->join(' · ') }}</div></td><td class="px-6 py-4"><span class="rounded-full px-2 py-1 text-xs font-medium text-white" style="background-color: {{ $lead->status->color }}">{{ $lead->status->name }}</span></td><td class="px-6 py-4">{{ $lead->source?->name ?? '—' }}</td><td class="px-6 py-4">{{ $lead->assignedStaff?->name ?? '—' }}</td><td class="px-6 py-4">{{ $lead->lead_value > 0 ? number_format((float)$lead->lead_value, 2, ',', '.') : '—' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('leads.show', $lead) }}" class="text-indigo-600">Lihat</a></td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-gray-500">Belum ada lead yang sesuai.</td></tr>@endforelse</tbody></table></div><div class="border-t px-6 py-4">{{ $leads->links() }}</div></div>
    </div></div>
</x-app-layout>
