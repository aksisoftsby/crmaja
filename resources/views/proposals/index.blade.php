<x-app-layout>
<x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Proposal</h2>@can('create', App\Models\Proposal::class)<a href="{{ route('proposals.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">Buat proposal</a>@endcan</div></x-slot>
<div class="py-8"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="overflow-hidden rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Nomor</th><th class="px-6 py-3">Subjek</th><th class="px-6 py-3">Pelanggan / Lead</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Total</th></tr></thead><tbody class="divide-y">@forelse($proposals as $proposal)<tr><td class="px-6 py-4"><a class="font-semibold text-indigo-600" href="{{ route('proposals.show',$proposal) }}">{{ $proposal->number }}</a></td><td class="px-6 py-4">{{ $proposal->subject }}</td><td class="px-6 py-4">{{ $proposal->client?->company_name ?? $proposal->lead?->name ?? '—' }}</td><td class="px-6 py-4">{{ ucfirst($proposal->status) }}</td><td class="px-6 py-4">{{ number_format((float)$proposal->total,2,',','.') }}</td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-gray-500">Belum ada proposal.</td></tr>@endforelse</tbody></table><div class="border-t px-6 py-4">{{ $proposals->links() }}</div></div></div></div>
</x-app-layout>
