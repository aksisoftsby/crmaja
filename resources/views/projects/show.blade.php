<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800">{{ $project->name }}</h2><p class="text-sm text-gray-500">{{ $project->client?->company_name ?? 'Tanpa pelanggan' }}</p></div><div class="flex items-center gap-4">@can('update', $project)<a href="{{ route('projects.edit', $project) }}" class="text-sm text-indigo-600">Edit</a>@endcan<a href="{{ route('projects.index') }}" class="text-sm text-gray-600">Kembali</a></div></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-6xl space-y-5 sm:px-6 lg:px-8"><div class="grid grid-cols-1 gap-5 md:grid-cols-4"><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Status</p><p class="mt-2 font-medium">{{ str($project->status)->replace('_', ' ')->title() }}</p></div><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Tipe penagihan</p><p class="mt-2 font-medium">{{ str($project->billing_type)->replace('_', ' ')->title() }}</p></div><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Progress</p><p class="mt-2 font-medium">{{ $project->progress }}%</p></div><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Tenggat</p><p class="mt-2 font-medium">{{ $project->deadline?->format('d M Y') ?? '—' }}</p></div></div>
        <div class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Deskripsi</h3><p class="mt-3 whitespace-pre-line text-sm text-gray-700">{{ $project->description ?: 'Tidak ada deskripsi.' }}</p><div class="mt-5 border-t pt-4 text-sm"><span class="font-medium">Anggota:</span> {{ $project->members->pluck('name')->join(', ') ?: '—' }}</div></div>
        <div class="rounded-lg bg-white p-6 shadow-sm"><div class="flex items-center justify-between"><h3 class="font-semibold text-gray-900">Task project</h3>@can('create', App\Models\Task::class)<a href="{{ route('tasks.create', ['project_id' => $project->id]) }}" class="text-sm text-indigo-600">Task baru</a>@endcan</div><div class="mt-4 overflow-hidden rounded border"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-4 py-3">Task</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Assignee</th><th class="px-4 py-3">Tenggat</th></tr></thead><tbody class="divide-y">@forelse($project->tasks as $task)<tr><td class="px-4 py-3"><a class="font-medium text-indigo-600" href="{{ route('tasks.show', $task) }}">{{ $task->name }}</a></td><td class="px-4 py-3">{{ str($task->status)->replace('_', ' ')->title() }}</td><td class="px-4 py-3">{{ $task->assignees->pluck('name')->join(', ') ?: '—' }}</td><td class="px-4 py-3">{{ $task->due_date?->format('d M Y') ?? '—' }}</td></tr>@empty<tr><td colspan="4" class="px-4 py-8 text-center text-gray-500">Belum ada task.</td></tr>@endforelse</tbody></table></div></div>
        <div class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Milestone</h3><div class="mt-4 space-y-3">@forelse($project->milestones as $milestone)<div class="flex items-start justify-between border-b pb-3 last:border-0"><div><p class="font-medium">{{ $milestone->title }}</p><p class="text-sm text-gray-500">{{ $milestone->due_date?->format('d M Y') ?? 'Tanpa tenggat' }} · {{ $milestone->tasks->count() }} task</p><p class="mt-1 text-sm text-gray-600">{{ $milestone->description }}</p></div>@can('update', $project)<form method="POST" action="{{ route('projects.milestones.destroy', [$project, $milestone]) }}">@csrf @method('DELETE')<button class="text-xs text-red-600">Hapus</button></form>@endcan</div>@empty<p class="text-sm text-gray-500">Belum ada milestone.</p>@endforelse</div>@can('update', $project)<form method="POST" action="{{ route('projects.milestones.store', $project) }}" class="mt-5 grid grid-cols-1 gap-3 border-t pt-5 md:grid-cols-4">@csrf <x-text-input name="title" class="block w-full" placeholder="Nama milestone" required/><x-text-input name="due_date" type="date" class="block w-full"/><x-text-input name="description" class="block w-full" placeholder="Deskripsi (opsional)"/><x-primary-button>Tambah milestone</x-primary-button></form>@endcan</div>
    </div></div>
</x-app-layout>
