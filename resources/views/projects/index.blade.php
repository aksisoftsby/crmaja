<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Projects</h2>@can('create', App\Models\Project::class)<a href="{{ route('projects.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white">Project baru</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="overflow-hidden rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Project</th><th class="px-6 py-3">Pelanggan</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Progress</th><th class="px-6 py-3">Tenggat</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y">@forelse($projects as $project)<tr><td class="px-6 py-4 font-medium text-gray-900">{{ $project->name }}<div class="mt-1 text-xs font-normal text-gray-500">{{ $project->members->pluck('name')->join(', ') }}</div></td><td class="px-6 py-4">{{ $project->client?->company_name ?? '—' }}</td><td class="px-6 py-4">{{ str($project->status)->replace('_', ' ')->title() }}</td><td class="px-6 py-4"><div class="h-2 w-28 overflow-hidden rounded bg-gray-200"><div class="h-full bg-indigo-600" style="width: {{ $project->progress }}%"></div></div><span class="mt-1 block text-xs text-gray-500">{{ $project->progress }}%</span></td><td class="px-6 py-4">{{ $project->deadline?->format('d M Y') ?? '—' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('projects.show', $project) }}" class="text-indigo-600">Buka</a></td></tr>@empty<tr><td colspan="6" class="px-6 py-10 text-center text-gray-500">Belum ada project.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $projects->links() }}</div></div></div>
</x-app-layout>
