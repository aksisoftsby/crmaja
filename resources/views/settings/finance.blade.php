<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">Pengaturan Finance</h2></x-slot><div class="py-8"><div class="mx-auto max-w-7xl space-y-5 sm:px-6 lg:px-8">@if(session('status'))<div class="rounded-md bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif<div class="grid grid-cols-1 gap-5 lg:grid-cols-3"><section class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Mata uang</h3><form method="POST" action="{{ route('settings.finance.currencies.store') }}" class="mt-4 space-y-3">@csrf <x-text-input name="code" class="block w-full" placeholder="Kode, contoh IDR" required/><x-text-input name="name" class="block w-full" placeholder="Nama" required/><x-text-input name="symbol" class="block w-full" placeholder="Simbol" required/><x-text-input name="decimal_places" type="number" min="0" max="6" value="2" class="block w-full" required/><x-primary-button>Tambah mata uang</x-primary-button></form><div class="mt-5 space-y-2 text-sm">@foreach($currencies as $currency)<div class="flex justify-between"><span>{{ $currency->code }} · {{ $currency->symbol }}</span><span class="text-gray-500">{{ $currency->is_default ? 'Default' : '' }}</span></div>@endforeach</div></section><section class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Pajak</h3><form method="POST" action="{{ route('settings.finance.taxes.store') }}" class="mt-4 space-y-3">@csrf <x-text-input name="name" class="block w-full" placeholder="Nama pajak" required/><x-text-input name="rate" type="number" min="0" max="100" step="0.0001" class="block w-full" placeholder="Tarif %" required/><x-primary-button>Tambah pajak</x-primary-button></form><div class="mt-5 space-y-2 text-sm">@foreach($taxes as $tax)<div class="flex justify-between"><span>{{ $tax->name }}</span><span class="text-gray-500">{{ $tax->rate }}%</span></div>@endforeach</div></section><section class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Metode pembayaran</h3><form method="POST" action="{{ route('settings.finance.payment-methods.store') }}" class="mt-4 space-y-3">@csrf <x-text-input name="name" class="block w-full" placeholder="Nama metode" required/><x-primary-button>Tambah metode</x-primary-button></form><div class="mt-5 space-y-2 text-sm">@foreach($paymentMethods as $method)<div class="flex justify-between"><span>{{ $method->name }}</span><span class="text-gray-500">{{ $method->is_active ? 'Aktif' : 'Nonaktif' }}</span></div>@endforeach</div></section></div></div></div></x-app-layout>
