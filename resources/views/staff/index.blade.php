<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800">Staff</h2><p class="mt-1 text-sm text-gray-500">Kelola akses dan status akun tim.</p></div><a href="{{ route('staff.create') }}" class="inline-flex items-center rounded-md bg-indigo-600 px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white hover:bg-indigo-500">Tambah staff</a></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 sm:px-6 lg:px-8">@if (session('status'))<div class="rounded-md bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif<div class="overflow-hidden bg-white shadow-sm sm:rounded-lg"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left text-xs font-semibold uppercase tracking-wider text-gray-500"><tr><th class="px-6 py-3">Staff</th><th class="px-6 py-3">Role</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Dibuat</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y divide-gray-200 bg-white text-gray-700">@forelse($staff as $member)<tr><td class="px-6 py-4"><div class="font-semibold text-gray-900">{{ $member->name }}</div><div class="text-xs text-gray-500">{{ $member->email }}</div></td><td class="px-6 py-4">{{ $member->roles->pluck('name')->join(', ') ?: 'Belum ada role' }}</td><td class="px-6 py-4"><span @class(['inline-flex rounded-full px-2 py-1 text-xs font-medium', 'bg-emerald-100 text-emerald-800' => $member->is_active, 'bg-gray-100 text-gray-700' => ! $member->is_active])>{{ $member->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td class="px-6 py-4 text-gray-500">{{ $member->created_at->format('d M Y') }}</td><td class="px-6 py-4 text-right"><a href="{{ route('staff.edit', $member) }}" class="text-sm font-medium text-indigo-600 hover:text-indigo-900">Ubah</a>@if($member->is_active && ! $member->hasRole('Super Admin'))<form method="POST" action="{{ route('staff.destroy', $member) }}" class="inline" onsubmit="return confirm('Nonaktifkan staff ini?')">@csrf @method('DELETE')<button class="ms-3 text-sm font-medium text-red-600 hover:text-red-800">Nonaktifkan</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-gray-500">Belum ada akun staff.</td></tr>@endforelse</tbody></table></div><div class="border-t border-gray-200 px-6 py-4">{{ $staff->links() }}</div></div></div></div>
</x-app-layout>
