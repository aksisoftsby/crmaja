<!doctype html>
<html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $title ?? 'Client Portal' }} — Aksisoft CRM</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head><body class="min-h-screen bg-slate-100"><nav class="border-b bg-white"><div class="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><a href="{{ route('portal.dashboard') }}" class="font-semibold text-slate-900">Aksisoft Client Portal</a><div class="hidden gap-4 text-sm md:flex"><a href="{{ route('portal.invoices') }}" class="text-slate-600">Invoice</a><a href="{{ route('portal.proposals') }}" class="text-slate-600">Proposal</a><a href="{{ route('portal.estimates') }}" class="text-slate-600">Estimate</a><a href="{{ route('portal.projects') }}" class="text-slate-600">Project</a><a href="{{ route('portal.tickets') }}" class="text-slate-600">Ticket</a><a href="{{ route('portal.knowledge-base') }}" class="text-slate-600">Knowledge Base</a></div><form method="POST" action="{{ route('portal.logout') }}">@csrf <button class="text-sm text-slate-600">Keluar</button></form></div></nav><main class="py-8">@if(session('status'))<div class="mx-auto mb-5 max-w-7xl rounded bg-emerald-50 px-5 py-3 text-sm text-emerald-800">{{ session('status') }}</div>@endif {{ $slot }}</main></body></html>
