<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Items</h2>@can('create', App\Models\Item::class)<a href="{{ route('items.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">Tambah item</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 sm:px-6 lg:px-8">@if(session('status'))<div class="rounded-md bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif<div class="rounded-lg bg-white p-6 shadow-sm"><form method="GET" class="flex gap-3"><x-text-input name="search" type="search" class="block w-full" :value="request('search')" placeholder="Cari item"/><x-primary-button>Cari</x-primary-button></form></div><div class="overflow-hidden rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Item</th><th class="px-6 py-3">Tarif</th><th class="px-6 py-3">Satuan</th><th class="px-6 py-3">Status</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y">@forelse($items as $item)<tr><td class="px-6 py-4"><div class="font-semibold">{{ $item->title }}</div><div class="text-xs text-gray-500">{{ $item->description }}</div></td><td class="px-6 py-4">{{ number_format((float)$item->rate, 2, ',', '.') }}</td><td class="px-6 py-4">{{ $item->unit }}</td><td class="px-6 py-4">{{ $item->is_active ? 'Aktif' : 'Nonaktif' }}</td><td class="px-6 py-4 text-right">@can('update',$item)<a href="{{ route('items.edit',$item) }}" class="text-indigo-600">Ubah</a>@endcan</td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-gray-500">Belum ada item.</td></tr>@endforelse</tbody></table><div class="border-t px-6 py-4">{{ $items->links() }}</div></div></div></div>
</x-app-layout>
