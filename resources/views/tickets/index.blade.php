<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Tickets</h2>@can('create', App\Models\Ticket::class)<a href="{{ route('tickets.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white">Ticket baru</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="overflow-hidden rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Ticket</th><th class="px-6 py-3">Pelanggan</th><th class="px-6 py-3">Departemen</th><th class="px-6 py-3">Prioritas</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Assignee</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y">@forelse($tickets as $ticket)<tr><td class="px-6 py-4"><div class="font-medium text-gray-900">{{ $ticket->subject }}</div><div class="text-xs text-gray-500">{{ $ticket->number }}</div></td><td class="px-6 py-4">{{ $ticket->client?->company_name ?? '—' }}</td><td class="px-6 py-4">{{ $ticket->department?->name ?? '—' }}</td><td class="px-6 py-4">{{ str($ticket->priority)->title() }}</td><td class="px-6 py-4">{{ str($ticket->status)->replace('_', ' ')->title() }}</td><td class="px-6 py-4">{{ $ticket->assignee?->name ?? '—' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('tickets.show', $ticket) }}" class="text-indigo-600">Buka</a></td></tr>@empty<tr><td colspan="7" class="px-6 py-10 text-center text-gray-500">Belum ada ticket.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $tickets->links() }}</div></div></div>
</x-app-layout>
