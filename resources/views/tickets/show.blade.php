<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800">{{ $ticket->subject }}</h2><p class="text-sm text-gray-500">{{ $ticket->number }}</p></div><div class="flex items-center gap-4">@can('update', $ticket)<a href="{{ route('tickets.edit', $ticket) }}" class="text-sm text-indigo-600">Edit</a>@endcan<a href="{{ route('tickets.index') }}" class="text-sm text-gray-600">Kembali</a></div></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-5xl space-y-5 sm:px-6 lg:px-8"><div class="grid grid-cols-1 gap-5 md:grid-cols-4"><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Status</p><p class="mt-2 font-medium">{{ str($ticket->status)->replace('_', ' ')->title() }}</p></div><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Prioritas</p><p class="mt-2 font-medium">{{ str($ticket->priority)->title() }}</p></div><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Pelanggan</p><p class="mt-2 font-medium">{{ $ticket->client?->company_name ?? '—' }}</p></div><div class="rounded-lg bg-white p-5 shadow-sm"><p class="text-xs uppercase text-gray-500">Assignee</p><p class="mt-2 font-medium">{{ $ticket->assignee?->name ?? '—' }}</p></div></div>
        <div class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Percakapan</h3><div class="mt-4 space-y-4">@forelse($ticket->replies as $reply)<div class="rounded border p-4 {{ $reply->is_internal_note ? 'border-amber-200 bg-amber-50' : 'border-gray-200' }}"><div class="flex justify-between"><p class="text-sm font-medium">{{ $reply->is_internal_note ? 'Catatan internal' : 'Staff' }}</p><span class="text-xs text-gray-500">{{ $reply->created_at->format('d M Y H:i') }}</span></div><p class="mt-2 whitespace-pre-line text-sm text-gray-700">{{ $reply->message }}</p></div>@empty<p class="text-sm text-gray-500">Belum ada pesan.</p>@endforelse</div><form method="POST" action="{{ route('tickets.replies.store', $ticket) }}" class="mt-5 border-t pt-5">@csrf <textarea name="message" rows="4" class="block w-full rounded-md border-gray-300 shadow-sm" placeholder="Tulis balasan atau catatan" required></textarea><label class="mt-3 flex items-center gap-2 text-sm"><input type="checkbox" name="is_internal_note" value="1" class="rounded border-gray-300 text-indigo-600 shadow-sm"> Catatan internal (tidak terlihat oleh client)</label><div class="mt-3"><x-primary-button>Kirim</x-primary-button></div></form></div>
        <div class="rounded-lg bg-white p-6 shadow-sm"><dl class="grid grid-cols-1 gap-4 text-sm md:grid-cols-3"><div><dt class="text-gray-500">Departemen</dt><dd class="mt-1 font-medium">{{ $ticket->department?->name ?? '—' }}</dd></div><div><dt class="text-gray-500">Kontak</dt><dd class="mt-1 font-medium">{{ $ticket->contact ? trim($ticket->contact->first_name.' '.$ticket->contact->last_name) : '—' }}</dd></div><div><dt class="text-gray-500">Sumber</dt><dd class="mt-1 font-medium">{{ str($ticket->source)->title() }}</dd></div></dl></div>
    </div></div>
</x-app-layout>
