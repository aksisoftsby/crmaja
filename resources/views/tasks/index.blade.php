<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Tasks</h2>@can('create', App\Models\Task::class)<a href="{{ route('tasks.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white">Task baru</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="overflow-hidden rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Task</th><th class="px-6 py-3">Project</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Prioritas</th><th class="px-6 py-3">Assignee</th><th class="px-6 py-3">Tenggat</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y">@forelse($tasks as $task)<tr><td class="px-6 py-4 font-medium text-gray-900">{{ $task->name }}</td><td class="px-6 py-4">{{ $task->related instanceof App\Models\Project ? $task->related->name : '—' }}</td><td class="px-6 py-4">{{ str($task->status)->replace('_', ' ')->title() }}</td><td class="px-6 py-4">{{ str($task->priority)->title() }}</td><td class="px-6 py-4">{{ $task->assignees->pluck('name')->join(', ') ?: '—' }}</td><td class="px-6 py-4">{{ $task->due_date?->format('d M Y') ?? '—' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('tasks.show', $task) }}" class="text-indigo-600">Buka</a></td></tr>@empty<tr><td colspan="7" class="px-6 py-10 text-center text-gray-500">Belum ada task.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $tasks->links() }}</div></div></div>
</x-app-layout>
