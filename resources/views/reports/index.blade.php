<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">Laporan</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 sm:px-6 lg:px-8"><div class="grid grid-cols-1 gap-5 md:grid-cols-2"><div class="rounded-lg bg-white p-6 shadow-sm"><p class="text-sm text-gray-500">Total tagihan</p><p class="mt-2 text-3xl font-semibold text-gray-900">{{ number_format((float) $totalBilled, 2, ',', '.') }}</p></div><div class="rounded-lg bg-white p-6 shadow-sm"><p class="text-sm text-gray-500">Total penerimaan</p><p class="mt-2 text-3xl font-semibold text-gray-900">{{ number_format((float) $totalReceived, 2, ',', '.') }}</p></div></div><div class="grid grid-cols-1 gap-5 xl:grid-cols-2"><section class="overflow-hidden rounded-lg bg-white shadow-sm"><h3 class="border-b px-6 py-4 font-semibold text-gray-900">Invoice menurut status</h3><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Status</th><th class="px-6 py-3">Jumlah</th><th class="px-6 py-3">Tagihan</th><th class="px-6 py-3">Terbayar</th></tr></thead><tbody class="divide-y">@forelse($invoiceByStatus as $row)<tr><td class="px-6 py-3">{{ str($row->status)->title() }}</td><td class="px-6 py-3">{{ $row->count }}</td><td class="px-6 py-3">{{ number_format((float) $row->total, 2, ',', '.') }}</td><td class="px-6 py-3">{{ number_format((float) $row->paid, 2, ',', '.') }}</td></tr>@empty<tr><td colspan="4" class="px-6 py-8 text-center text-gray-500">Belum ada invoice.</td></tr>@endforelse</tbody></table></section><section class="overflow-hidden rounded-lg bg-white shadow-sm"><h3 class="border-b px-6 py-4 font-semibold text-gray-900">Pelanggan berdasarkan nilai invoice</h3><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Pelanggan</th><th class="px-6 py-3 text-right">Nilai</th></tr></thead><tbody class="divide-y">@forelse($topClients as $client)<tr><td class="px-6 py-3">{{ $client->company_name }}</td><td class="px-6 py-3 text-right">{{ number_format((float) ($client->invoices_sum_total ?? 0), 2, ',', '.') }}</td></tr>@empty<tr><td colspan="2" class="px-6 py-8 text-center text-gray-500">Belum ada pelanggan.</td></tr>@endforelse</tbody></table></section></div><div class="grid grid-cols-1 gap-5 lg:grid-cols-3"><section class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Lead per status</h3><div class="mt-4 space-y-2">@foreach($leadByStatus as $row)<div class="flex justify-between text-sm"><span>{{ $row->name }}</span><span class="font-medium">{{ $row->leads_count }}</span></div>@endforeach</div></section><section class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Project per status</h3><div class="mt-4 space-y-2">@foreach($projectsByStatus as $row)<div class="flex justify-between text-sm"><span>{{ str($row->status)->replace('_', ' ')->title() }}</span><span class="font-medium">{{ $row->count }}</span></div>@endforeach</div></section><section class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold text-gray-900">Ticket per status</h3><div class="mt-4 space-y-2">@foreach($ticketsByStatus as $row)<div class="flex justify-between text-sm"><span>{{ str($row->status)->replace('_', ' ')->title() }}</span><span class="font-medium">{{ $row->count }}</span></div>@endforeach</div></section></div></div></div>
</x-app-layout>
