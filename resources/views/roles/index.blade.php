<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800">Role & Permission</h2><p class="mt-1 text-sm text-gray-500">Kelola peran akses staff CRM.</p></div><a href="{{ route('roles.create') }}" class="inline-flex items-center rounded-md bg-indigo-600 px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white hover:bg-indigo-500">Tambah role</a></div></x-slot><div class="py-8"><div class="mx-auto max-w-7xl space-y-5 sm:px-6 lg:px-8">@if(session('status'))<div class="rounded-md bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif<div class="overflow-hidden bg-white shadow-sm sm:rounded-lg"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left text-xs font-semibold uppercase tracking-wider text-gray-500"><tr><th class="px-6 py-3">Role</th><th class="px-6 py-3">Staff</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y divide-gray-200">@foreach($roles as $role)<tr><td class="px-6 py-4 font-semibold text-gray-900">{{ $role->name }}</td><td class="px-6 py-4 text-gray-600">{{ $role->users_count }}</td><td class="px-6 py-4 text-right">@if($role->name !== 'Super Admin')<a href="{{ route('roles.edit', $role) }}" class="text-sm font-medium text-indigo-600 hover:text-indigo-900">Ubah</a><form method="POST" action="{{ route('roles.destroy', $role) }}" class="inline" onsubmit="return confirm('Hapus role ini?')">@csrf @method('DELETE')<button class="ms-3 text-sm font-medium text-red-600 hover:text-red-800">Hapus</button></form>@else<span class="text-xs text-gray-500">Terkunci</span>@endif</td></tr>@endforeach</tbody></table></div></div></div></x-app-layout>
