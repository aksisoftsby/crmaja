<x-portal.layout title="Project">
    <div class="mx-auto max-w-7xl px-5">
        <p class="text-sm font-semibold text-teal-700">Pelaksanaan pekerjaan</p><h1 class="text-2xl font-extrabold tracking-tight text-slate-900">Project</h1><p class="mt-1 text-sm text-slate-500">Pantau progres, milestone, dan tugas yang dibagikan untuk project Anda.</p>
        <div class="mt-6 grid grid-cols-1 gap-5 md:grid-cols-2">@forelse($projects as $project)<article class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div class="flex items-start justify-between gap-4"><div><h2 class="font-extrabold text-slate-900">{{ $project->name }}</h2><p class="mt-1 text-sm font-semibold text-teal-700">{{ str($project->status)->replace('_', ' ')->title() }}</p></div><span class="text-lg font-extrabold text-slate-900">{{ $project->progress }}%</span></div><p class="mt-4 line-clamp-3 text-sm leading-6 text-slate-600">{{ $project->description }}</p><div class="mt-5 h-2.5 overflow-hidden rounded-full bg-slate-200"><div class="h-full rounded-full bg-teal-600" style="width: {{ min(100, max(0, $project->progress)) }}%"></div></div><div class="mt-4 flex items-center justify-between gap-3 text-xs font-medium text-slate-500"><span>{{ $project->tasks->count() }} task</span><span>Tenggat {{ $project->deadline?->format('d M Y') ?? '—' }}</span></div><a href="{{ route('portal.projects.show', $project) }}" class="mt-5 inline-flex w-full items-center justify-center rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-bold text-white transition hover:bg-teal-700">Lihat Detail Project</a></article>@empty<p class="rounded-2xl bg-white p-8 text-center text-slate-500 shadow-sm ring-1 ring-slate-200 md:col-span-2">Belum ada project yang dibagikan.</p>@endforelse</div><div class="mt-4">{{ $projects->links() }}</div>
    </div>
</x-portal.layout>
