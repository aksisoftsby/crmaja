<x-portal.layout title="Project"><div class="mx-auto max-w-7xl px-5"><h1 class="text-2xl font-semibold text-slate-900">Project</h1><div class="mt-6 grid grid-cols-1 gap-5 md:grid-cols-2">@forelse($projects as $project)<article class="rounded-lg bg-white p-6 shadow-sm"><div class="flex items-start justify-between"><div><h2 class="font-semibold text-slate-900">{{ $project->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ str($project->status)->replace('_', ' ')->title() }}</p></div><span class="text-lg font-semibold text-indigo-600">{{ $project->progress }}%</span></div><p class="mt-4 line-clamp-3 text-sm text-slate-600">{{ $project->description }}</p><div class="mt-5 h-2 overflow-hidden rounded bg-slate-200"><div class="h-full bg-indigo-600" style="width: {{ $project->progress }}%"></div></div><p class="mt-3 text-xs text-slate-500">{{ $project->tasks->count() }} task · Tenggat {{ $project->deadline?->format('d M Y') ?? '—' }}</p><div class="mt-4 border-t pt-3"><p class="text-xs font-medium uppercase text-slate-500">Task</p><ul class="mt-2 space-y-1">@forelse($project->tasks as $task)<li class="flex justify-between gap-3 text-sm text-slate-700"><span>{{ $task->name }}</span><span class="text-xs text-slate-500">{{ str($task->status)->replace('_', ' ')->title() }}</span></li>@empty<li class="text-sm text-slate-500">Belum ada task.</li>@endforelse</ul></div></article>@empty<p class="rounded-lg bg-white p-8 text-center text-slate-500 shadow-sm md:col-span-2">Belum ada project yang dibagikan.</p>@endforelse</div><div class="mt-4">{{ $projects->links() }}</div></div></x-portal.layout>
