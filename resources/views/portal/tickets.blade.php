<x-portal.layout title="Ticket"><div class="mx-auto max-w-7xl px-5"><div class="grid grid-cols-1 gap-6 lg:grid-cols-3"><section class="lg:col-span-2"><h1 class="text-2xl font-semibold text-slate-900">Ticket Dukungan</h1><div class="mt-6 overflow-hidden rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Ticket</th><th class="px-5 py-3">Departemen</th><th class="px-5 py-3">Prioritas</th><th class="px-5 py-3">Status</th></tr></thead><tbody class="divide-y">@forelse($tickets as $ticket)<tr><td class="px-5 py-4"><div class="font-medium">{{ $ticket->subject }}</div><div class="text-xs text-slate-500">{{ $ticket->number }}</div><div class="mt-2 text-xs text-slate-600">{{ $ticket->replies->last()?->message }}</div></td><td class="px-5 py-4">{{ $ticket->department?->name ?? '—' }}</td><td class="px-5 py-4">{{ str($ticket->priority)->title() }}</td><td class="px-5 py-4">{{ str($ticket->status)->replace('_', ' ')->title() }}</td></tr>@empty<tr><td colspan="4" class="px-5 py-10 text-center text-slate-500">Belum ada ticket.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $tickets->links() }}</div></section><aside class="rounded-lg bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Buat ticket baru</h2><form method="POST" action="{{ route('portal.tickets.store') }}" class="mt-4 space-y-4">@csrf <div><x-input-label for="subject" value="Subjek"/><x-text-input id="subject" name="subject" class="mt-1 block w-full" required/></div><div><x-input-label for="department_id" value="Departemen"/><select id="department_id" name="department_id" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm"><option value="">Pilih departemen</option>@foreach($departments as $department)<option value="{{ $department->id }}">{{ $department->name }}</option>@endforeach</select></div><div><x-input-label for="priority" value="Prioritas"/><select id="priority" name="priority" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm"><option value="medium">Sedang</option><option value="low">Rendah</option><option value="high">Tinggi</option><option value="urgent">Mendesak</option></select></div><div><x-input-label for="message" value="Pesan"/><textarea id="message" name="message" rows="5" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm" required></textarea></div><x-primary-button>Kirim ticket</x-primary-button></form></aside></div></div></x-portal.layout>
