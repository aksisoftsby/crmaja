<x-portal.layout :title="$article->title"><article class="mx-auto max-w-4xl rounded-lg bg-white p-8 shadow-sm"><a href="{{ route('portal.knowledge-base') }}" class="text-sm text-indigo-600">Kembali ke Knowledge Base</a><p class="mt-6 text-xs text-indigo-600">{{ $article->category?->name ?? 'Umum' }}</p><h1 class="mt-2 text-3xl font-semibold text-slate-900">{{ $article->title }}</h1><div class="mt-8 whitespace-pre-line text-sm leading-7 text-slate-700">{{ $article->content }}</div></article></x-portal.layout>
