<x-portal.layout title="Knowledge Base"><div class="mx-auto max-w-7xl px-5"><h1 class="text-2xl font-semibold text-slate-900">Knowledge Base</h1><div class="mt-6 grid grid-cols-1 gap-5 md:grid-cols-2 lg:grid-cols-3">@forelse($articles as $article)<a href="{{ route('portal.knowledge-base.show', $article) }}" class="rounded-lg bg-white p-6 shadow-sm transition hover:shadow"><p class="text-xs text-indigo-600">{{ $article->category?->name ?? 'Umum' }}</p><h2 class="mt-2 font-semibold text-slate-900">{{ $article->title }}</h2><p class="mt-3 line-clamp-3 text-sm text-slate-600">{{ $article->content }}</p><p class="mt-4 text-xs text-slate-400">{{ $article->views_count }} kunjungan</p></a>@empty<p class="rounded-lg bg-white p-8 text-center text-slate-500 shadow-sm md:col-span-2 lg:col-span-3">Belum ada artikel.</p>@endforelse</div><div class="mt-4">{{ $articles->links() }}</div></div></x-portal.layout>
