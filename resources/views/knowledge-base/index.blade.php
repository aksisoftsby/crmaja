<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Knowledge Base</h2>@can('create', App\Models\KbArticle::class)<a href="{{ route('kb-articles.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white">Artikel baru</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 sm:px-6 lg:px-8">@can('create', App\Models\KbArticle::class)<form method="POST" action="{{ route('kb-categories.store') }}" class="rounded-lg bg-white p-5 shadow-sm">@csrf <div class="flex flex-col gap-3 md:flex-row"><x-text-input name="name" class="block w-full" placeholder="Kategori baru" required/><x-primary-button>Tambah kategori</x-primary-button></div></form>@endcan<div class="overflow-hidden rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-6 py-3">Artikel</th><th class="px-6 py-3">Kategori</th><th class="px-6 py-3">Akses</th><th class="px-6 py-3">Publikasi</th><th class="px-6 py-3">Dilihat</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y">@forelse($articles as $article)<tr><td class="px-6 py-4 font-medium text-gray-900">{{ $article->title }}</td><td class="px-6 py-4">{{ $article->category?->name ?? '—' }}</td><td class="px-6 py-4">{{ $article->is_client_only ? 'Client' : 'Publik' }}</td><td class="px-6 py-4">{{ $article->is_published ? 'Published' : 'Draft' }}</td><td class="px-6 py-4">{{ $article->views_count }}</td><td class="px-6 py-4 text-right"><a href="{{ route('kb-articles.show', $article) }}" class="text-indigo-600">Buka</a></td></tr>@empty<tr><td colspan="6" class="px-6 py-10 text-center text-gray-500">Belum ada artikel.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $articles->links() }}</div></div></div>
</x-app-layout>
