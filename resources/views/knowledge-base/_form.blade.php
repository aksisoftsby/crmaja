<div class="grid grid-cols-1 gap-5 md:grid-cols-2">
    <div class="md:col-span-2"><x-input-label for="title" value="Judul artikel"/><x-text-input id="title" name="title" class="mt-1 block w-full" :value="old('title', $article->title ?? '')" required autofocus/><x-input-error :messages="$errors->get('title')" class="mt-2"/></div>
    <div><x-input-label for="category_id" value="Kategori"/><select id="category_id" name="category_id" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm"><option value="">Tanpa kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id', $article->category_id ?? '') == $category->id)>{{ $category->name }}</option>@endforeach</select></div>
    <div class="flex items-end gap-6 pb-2"><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="is_published" value="1" @checked(old('is_published', $article->is_published ?? false)) class="rounded border-gray-300 text-indigo-600 shadow-sm"> Publikasikan</label><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="is_client_only" value="1" @checked(old('is_client_only', $article->is_client_only ?? false)) class="rounded border-gray-300 text-indigo-600 shadow-sm"> Khusus client</label></div>
    <div class="md:col-span-2"><x-input-label for="content" value="Isi artikel"/><textarea id="content" name="content" rows="14" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm" required>{{ old('content', $article->content ?? '') }}</textarea></div>
</div>
<div class="mt-6 flex gap-3"><x-primary-button>{{ isset($article) ? 'Simpan perubahan' : 'Buat artikel' }}</x-primary-button><a href="{{ isset($article) ? route('kb-articles.show', $article) : route('kb-articles.index') }}" class="rounded-md px-4 py-2 text-sm text-gray-600">Batal</a></div>
