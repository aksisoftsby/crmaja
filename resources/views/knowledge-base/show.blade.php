<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800">{{ $article->title }}</h2><p class="text-sm text-gray-500">{{ $article->category?->name ?? 'Tanpa kategori' }}</p></div><div class="flex items-center gap-4">@can('update', $article)<a href="{{ route('kb-articles.edit', $article) }}" class="text-sm text-indigo-600">Edit</a>@endcan<a href="{{ route('kb-articles.index') }}" class="text-sm text-gray-600">Kembali</a></div></div></x-slot>
    <div class="py-8"><article class="mx-auto max-w-4xl rounded-lg bg-white p-8 shadow-sm"><div class="mb-6 flex gap-3 text-xs"><span class="rounded bg-gray-100 px-2 py-1">{{ $article->is_published ? 'Published' : 'Draft' }}</span><span class="rounded bg-gray-100 px-2 py-1">{{ $article->is_client_only ? 'Khusus client' : 'Publik' }}</span><span class="rounded bg-gray-100 px-2 py-1">{{ $article->views_count }} kunjungan</span></div><div class="whitespace-pre-line text-sm leading-7 text-gray-700">{{ $article->content }}</div></article></div>
</x-app-layout>
